import BucketProps from 'bucket_props';
import RiakConfig from 'riak_config';
import RiakStatus from 'riak_status';

let RiakHelp = {
  BucketProps: BucketProps,
  RiakConfig: RiakConfig,
  RiakStatus: RiakStatus
};

export default RiakHelp;
